import React from 'react';

const ComplaintLink = () => {
    return (
        <div>
            <p>Don't like the weather?</p>
            <a href="#">Complain to the weather person!</a>
        </div>
    )
}

export default ComplaintLink;
